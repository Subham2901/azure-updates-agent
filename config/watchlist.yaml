# Products must match Microsoft's taxonomy EXACTLY (validated at startup).
# Discover names: uv run python scripts/debug_filter.py
products:
  - "Azure Kubernetes Service (AKS)"
  - "Azure Firewall"
  - "Azure SQL Database"

# Only report updates carrying at least one of these tags.
# Empty list = all tags.
tags:
  - "Retirements"
  - "Features"
  - "Security"